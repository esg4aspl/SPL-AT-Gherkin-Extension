
/**
 * Determine that given root path includes maven project or not.
 * @param rootPath root path
 */
export function isMavenProject(rootPath: string | undefined): boolean{
    if(!rootPath){
        return false;
    }
    return false;
}
